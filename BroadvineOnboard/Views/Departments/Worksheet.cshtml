@model BroadvineOnboard.Models.Property

@{
    ViewBag.Title = "Select Worksheet";

    IEnumerable<SelectListItem> worksheetNames = ViewBag.WorkSheetNames;

    int rowNumber = 0;

}

<h2>Select a Worksheet</h2>

@using (Html.BeginForm())
{
    @*<p>
        Your spreadsheet contains more than one worksheet. Please select the worksheet to import from.
    </p>

    @Html.DropDownList("worksheetName", worksheetNames, htmlAttributes: new { @class = "form-control", @style = "display:inline-block;" })


    <p>
        Which row number in this worksheet is the header row?
    </p>

    <input type="text" class="form-control" />


    <input type="submit" value="Next" class="btn btn-default" style="display: inline-block;"/>*@

    <div class="form-group">
        <label for="worksheet-dropdown">Your spreadsheet contains more than one worksheet. Please select the worksheet to import from.</label>
        @Html.DropDownList("worksheetName", worksheetNames, htmlAttributes: new { id = "worksheet-dropdown", @class = "form-control" })
    </div>
    <div class="form-group">
        <label for="worksheet-row">Which row number in this worksheet is the header row?</label>
        @Html.TextBox("rowNumber", rowNumber, new { id = "worksheet-row", @class = "form-control" })
        @*<input class="form-control" id="worksheet-row" type="text">*@
    </div>
    <div>
        <input type="submit" value="Next" class="btn btn-default" style="display: inline-block;" />
    </div>
}
