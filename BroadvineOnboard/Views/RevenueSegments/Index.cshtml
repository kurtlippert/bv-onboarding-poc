@model IEnumerable<BroadvineOnboard.Models.RevenueSegment>

@{
    ViewBag.Title = "Index";
}

<h2>Revenue Segment Listing</h2>

<p>
    @Html.ActionLink("Create New Revenue Segment", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RevenueSegmentGroup.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RevenueSegmentGroup.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td align="right">
            @Html.ActionLink("Edit", "Edit", new { id=item.RevenueSegmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RevenueSegmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RevenueSegmentID })
        </td>
    </tr>
}

</table>
